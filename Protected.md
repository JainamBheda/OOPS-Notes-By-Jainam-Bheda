## Protected access modifiers 

- So Protected access modifiers makes member class and attribute accessible
  1. Within Same Package 
  2. In Subclass(child classes), even if they are in different package.

Example: